spring:
  application:
    name: smartsplit

  datasource:
    url: jdbc:postgresql://localhost:5432/smartsplit
    username: postgres
    password: Anjali123
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update # temporary for early dev
    show-sql: true
    properties:
      hibernate:
        format_sql: true
    database-platform: org.hibernate.dialect.PostgreSQLDialect

  flyway:
    enabled: true
    locations: classpath:db/migration
